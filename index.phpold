<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Blog Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Ensure the sidebar remains sticky */
        <style>
    /* Ensure the sidebar remains sticky */
    .sticky-sidebar {
        position: sticky;
        top: 20px; /* Adjust the distance from the top as needed */
        height: calc(100vh - 100px); /* Adjust the height based on your layout */
        overflow-y: auto;
    }

    .sticky-sidebar .card {
        margin-bottom: 15px;
    }

    /* Create a two-column layout */
    .two-column-layout {
        display: flex;
    }

    .main-content {
        flex-grow: 1;
    }

    @media (max-width: 768px) {
        /* Adjust the layout for smaller screens */
        .two-column-layout {
            flex-direction: column;
        }

        .sticky-sidebar {
            position: -webkit-sticky; /* For Safari */
            position: sticky;
            top: 20px; /* Adjust the distance from the top as needed */
            height: calc(100vh - 100px); /* Adjust the height based on your layout */
            overflow-y: auto;
        }
    }
</style>

</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Your Blog Name</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <!-- Add more navbar items as needed -->
            </ul>
        </div>
    </div>
</nav>

<!-- Page content -->
<div class="container mt-5">
    <div class="row two-column-layout">
        <!-- Blog content -->
        <div class="col-md-8 main-content">
            <?php
            require_once 'includes/config.php';

            // Fetch blog posts from the database
            $sql = "SELECT * FROM posts";
            $result = mysqli_query($conn, $sql);

            if (mysqli_num_rows($result) > 0) {
                // Output data of each row
                $firstPost = true;
                while ($row = mysqli_fetch_assoc($result)) {
                    // Display posts
                    echo '<div class="card mb-3">';
                    if ($firstPost) {
                        // Show the first post in full
                        echo '<img src="' . $row["image"] . '" class="card-img-top" alt="...">';
                        echo '<div class="card-body">';
                        echo '<h5 class="card-title">' . $row["title"] . '</h5>';
                        echo '<p class="card-text">' . $row["content"] . '</p>';
                        echo '</div>';
                        $firstPost = false;
                    } else {
                        // Show other posts with a maximum of 10 words and no image if there are none
                        echo '<div class="card-body">';
                        echo '<h5 class="card-title">' . $row["title"] . '</h5>';
                        echo '<p class="card-text">';
                        $contentWords = explode(' ', $row["content"]);
                        echo implode(' ', array_slice($contentWords, 0, 10)) . '...';
                        echo '</p>';
                        echo '</div>';
                    }
                    echo '</div>';
                }
            } else {
                echo "0 results";
            }

            // Close database connection
           
            ?>
        </div>

        <!-- Sidebar -->
        <div class="col-md-4 sticky-sidebar" id="stickySidebar">
            <?php
            require_once 'includes/config.php';

            // Fetch categories from the database
            $sql = "SELECT * FROM categories";
            $result = mysqli_query($conn, $sql);
            $categories = mysqli_fetch_all($result, MYSQLI_ASSOC);

            // Fetch tags from the database
            $sql = "SELECT * FROM tags";
            $result = mysqli_query($conn, $sql);
            $tags = mysqli_fetch_all($result, MYSQLI_ASSOC);

            ?>

            <?php if (count($categories) > 0 || count($tags) > 0) : ?>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Categories</h5>
                        <ul class="list-group">
                            <?php foreach ($categories as $category) : ?>
                                <li class="list-group-item"><?php echo $category['name']; ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </div>
                </div>

                <div class="card mt-4">
                    <div class="card-body">
                        <h5 class="card-title">Tags</h5>
                        <ul class="list-group">
                            <?php foreach ($tags as $tag) : ?>
                                <li class="list-group-item"><?php echo $tag['name']; ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </div>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<!-- Footer -->
<footer class="bg-light text-center text-lg-start mt-5">
    <div class="text-center p-3">
        © Your Blog Name - <?php echo date("Y"); ?>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Adjust the position of the sticky sidebar dynamically
    window.addEventListener('scroll', function() {
        var stickySidebar = document.getElementById('stickySidebar');
        var sidebarTop = stickySidebar.getBoundingClientRect().top;

        if (sidebarTop <= 20) {
            stickySidebar.style.top = (20 - sidebarTop) + 'px';
        } else {
            stickySidebar.style.top = '20px';
        }
    });
</script>
</body>
</html>
